{
  "name": "location-photobooth",
  "version": "1.0.0",
  "description": "Site vitrine location photobooth — structure nationale scalable",
  "scripts": {
    "dev": "vite",
    "build": "cd nextjs && npm install && npm run build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^6.0.0"
  }
}
