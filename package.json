{
  "name": "location-photobooth",
  "version": "1.0.0",
  "scripts": {
    "dev": "npm --prefix nextjs run dev",
    "build": "npm --prefix nextjs run build"
  },
  "dependencies": {
    "next": "^15.2.0"
  }
}
